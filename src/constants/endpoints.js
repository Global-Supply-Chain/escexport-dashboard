export const baseURL = "http://localhost:8000/dashboard";

export const endpoints = {
    login: "auth/login",
    admin: "admin",
    user: "user",
    promotion: "promotion",
    image: `${baseURL}/media`,
    status: "status",
}